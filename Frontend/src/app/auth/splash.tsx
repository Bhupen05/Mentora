/**
 * Splash Route
 * Entry point for the app - loads user session and navigates accordingly
 */

import { SplashScreen } from "@/features/splash/screens/SplashScreen";

export default SplashScreen;
